{
	"name": "mascot-web",
	"version": "0.0.0",
	"scripts": {
		"postinstall": "cd lib/CubismWebFramework && npm install && npm run build || echo done.",
		"dev": "vite",
		"tsc": "vue-tsc --noEmit",
		"build": "vite build",
		"serve": "vite preview"
	},
	"dependencies": {
		"vue": "^3.2.27"
	},
	"devDependencies": {
		"@vitejs/plugin-vue": "^2.0.1",
		"@vue/compiler-sfc": "^3.2.22",
		"typescript": "^4.5.4",
		"vite": "^2.7.10",
		"vue-tsc": "^0.30.1"
	}
}
